# E0258: Invalid Interface Implementation

## Explanation

An `impl Interface for Type` block does not satisfy the interface contract. The
interface must exist, and every required method must be implemented with matching
type parameters, parameter count, parameter mutability, parameter types, and
return type.

## Error Example

```nomo
package app.main

interface Display {
    fn to_string(self) -> string
}

struct User {
    name: string
}

impl Display for User {
    fn to_string(self) -> i32 {
        return 1
    }
}
```

## Correct Code

```nomo
package app.main

interface Display {
    fn to_string(self) -> string
}

struct User {
    name: string
}

impl Display for User {
    fn to_string(self) -> string {
        return self.name
    }
}
```

## Fix

Make the impl method list and signatures match the interface declaration. In
v0.1, `Self` in an interface method is checked against the concrete impl target
type.

## LSP Code Action

No automatic code action is currently provided.

## Related Links

- [README: interface and FFI](../../README.md#language-notes)
- [Compiler source](../../src/compiler.rs)

package app.main

import std.io
import std.task

suspend fn main() -> void {
    io.println("before yield")
    task.yield_now()
    io.println("after first yield")
    task.yield_now()
    io.println("after second yield")
}

package defer_question

import std.io

fn fail_label() -> Result<string, string> {
    return Err("fail")
}

fn cleanup(label: string) {
    io.println(label)
}

fn run() -> Result<string, string> {
    defer cleanup("defer before ? error")
    let label: string = fail_label()?
    return Ok(label)
}

fn main() {
    let result: Result<string, string> = run()
    match result {
        Ok(label) => {
            io.println(label)
        }
        Err(err) => {
            io.println(err)
        }
    }
}

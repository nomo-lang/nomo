package ffi_puts

import std.ffi

extern "C" {
    fn puts(message: CString) -> i32
}

fn main() {
    let message: CString = CString.from_string("ffi puts ok")
    unsafe {
        puts(message)
    }
}

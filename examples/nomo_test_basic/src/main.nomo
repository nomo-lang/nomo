package nomo_test_basic

import std.io
import std.testing

fn add(a: i64, b: i64) -> i64 {
    return a + b
}

#[test]
fn adds_numbers() {
    testing.assert_equal(add(20, 22), 42)
}

fn main() {
    io.println("nomo test basic ok")
}

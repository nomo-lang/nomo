package cli

import core.math
import std.io
import std.testing

/// Runs the workspace CLI example.
pub fn run_cli() {
    io.println(workspace_label())
}

#[test]
fn cli_uses_core_math() {
    testing.assert_equal(add(20, 22), 42)
}

fn main() {
    run_cli()
}
